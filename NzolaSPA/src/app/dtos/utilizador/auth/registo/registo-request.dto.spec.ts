import { RegistoRequestDto } from './registo-request.dto';

describe('RegistoRequestDto', () => {
  it('should create an instance', () => {
    expect(new RegistoRequestDto()).toBeTruthy();
  });
});
