import { RequisicaoCriarDto } from './requisicao-criar.dto';

describe('RequisicaoCriarDto', () => {
  it('should create an instance', () => {
    expect(new RequisicaoCriarDto()).toBeTruthy();
  });
});
