import { Component } from '@angular/core';
import { FontAwesomeModule } from '@fortawesome/angular-fontawesome';

@Component({
  selector: 'app-feed',
  imports: [FontAwesomeModule],
  templateUrl: './feed.html',
  styleUrl: './feed.css',
})
export class Feed {
  //notificarIcon = faBell;
}
